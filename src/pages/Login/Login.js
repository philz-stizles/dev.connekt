import React from 'react';
import { LoginWrapper } from './Login.styles';

const Login = (props) => {
  return (
    <LoginWrapper>
      <h1>github user</h1>
    </LoginWrapper>
  );
};

export default Login;
