import React from 'react';
import { PageWrapperWrapper } from './PageWrapper.styles';

const PageWrapper = ({ children }) => {
  return <PageWrapperWrapper>{children}</PageWrapperWrapper>;
};

export default PageWrapper;
